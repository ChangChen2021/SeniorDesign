package main.util;

/**
 * Enum for all field properties
 * 
 * @author Manuela Kastner
 *
 */
public enum FieldProperty {
	PROPERTIES, VALUE, RADIUS, PROPORTIONALITY, ROTATION_DIRECTION, ID, NAME
}
