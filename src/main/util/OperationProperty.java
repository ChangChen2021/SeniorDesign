package main.util;

/**
 * Enum for all operation properties.
 * 
 * @author Manuela Kastner
 *
 */
public enum OperationProperty {
	OPERATIONS, ROTATION_POINT_X, ROTATION_POINT_Y, FACTOR, AXIS, ALONG_X, ALONG_Y, DEGREE
}
